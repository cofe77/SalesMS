import { GoodsPricePipe } from './goods-price.pipe';

describe('GoodsPricePipe', () => {
  it('create an instance', () => {
    const pipe = new GoodsPricePipe();
    expect(pipe).toBeTruthy();
  });
});
