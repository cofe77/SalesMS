import { AllGoodsNamePipe } from './all-goods-name.pipe';

describe('AllGoodsNamePipe', () => {
  it('create an instance', () => {
    const pipe = new AllGoodsNamePipe();
    expect(pipe).toBeTruthy();
  });
});
