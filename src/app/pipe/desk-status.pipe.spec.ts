import { DeskStatusPipe } from './desk-status.pipe';

describe('DeskStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DeskStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
