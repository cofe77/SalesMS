import { GoodsNamePipe } from './goods-name.pipe';

describe('GoodsNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GoodsNamePipe();
    expect(pipe).toBeTruthy();
  });
});
